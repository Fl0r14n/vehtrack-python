/* global angular */

'use strict';

angular.module('homepage.footer', ['utils']).config(function() {
});

angular.module('homepage.footer').controller('footerController', function($scope) {
    var self = this;
}); 